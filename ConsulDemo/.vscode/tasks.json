{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Api1/Api1.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}